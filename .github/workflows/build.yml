name: Build

on:
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - target: aarch64-linux-android
            artifact: resetprop-arm64-v8a
          - target: armv7-linux-androideabi
            artifact: resetprop-armeabi-v7a
          - target: x86_64-linux-android
            artifact: resetprop-x86_64
          - target: i686-linux-android
            artifact: resetprop-x86
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Install NDK
        uses: nttld/setup-ndk@v1
        id: ndk
        with:
          ndk-version: r26d

      - name: Add NDK to PATH
        run: |
          NDK=${{ steps.ndk.outputs.ndk-path }}
          echo "$NDK/toolchains/llvm/prebuilt/linux-x86_64/bin" >> $GITHUB_PATH

      - name: Build
        env:
          RESETPROP_FRAMEWORK_SHA256: "f598cdde3ea9a693eac4889ad1a06d035ba607936fa1ccdaa1d3408f3839c608"
        run: cargo build --release --target ${{ matrix.target }}

      - name: Upload binary
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: target/${{ matrix.target }}/release/resetprop
